cd ./frontend/web

Remove-Item -Recurse -Force node_modules

Remove-Item -Force package-lock.json

npm cache verify(看到如这样的就是成功：Cache verified and compressed (~\AppData\Local\npm-cache\_cacache)
Content verified: 833 (315301122 bytes)
Content garbage-collected: 3 (5392849 bytes)
Index entries: 833
Finished in 3.656s)

npm install（看到如这样的就是成功：added 189 packages, and audited 190 packages in 49s

96 packages are looking for funding
  run `npm fund` for details

2 vulnerabilities (1 low, 1 moderate)

To address all issues, run:
  npm audit fix

Run `npm audit` for details.）

接着：
cd ../..
docker compose -f deploy/docker/docker-compose.yml down

Remove-Item `
  .\storage\agent.db, `
  .\storage\agent.db-wal, `
  .\storage\agent.db-shm `
  -Force -ErrorAction SilentlyContinue

Test-Path .\storage\agent.db(只要这个输出False，上面三步都算成功)

最后是
Copy-Item .\config\llm.local.example.json .\config\llm.local.json
然后再config/llm.local.json的api_key一栏输入我给的密钥

