# Test fixtures

Place non-secret, test-only fixture data here. Runtime copies of production
configuration are created under the build tree and are never written back to
the source `config` directory.
