接下来：按照以下思路升级。
1. 增强skill_txt.exe，使它能够生成/读取bat批处理和json文件，并允许txt.json专家使用指令直接生成bat/json文件（如WRITE -b xxx或WRITE -j xxx）。
2. 增加新的系统级条目：持续为上下文注入当前所在的workfolder。这个条目将会由用户手动指定，一般AI无权更改（除了第四点提到的mask_manager）。txt专家（应该改名叫文本编辑专家了）的默认行为都会以workfolder为基准。
3. 新增command masks掩码系统，和workfolder同步协作：每一个workfolder内可选地部注册一个文件，程序切换folder的时候进行读取——不可以切换时自动创建，必须由用户明确说明创建，免得把小文件塞得到处都是。读取过后，主程序将会对所有指令进行掩码运算，忽略被blocked的危险指令，相当于基于文件夹的“管理员权限”。如果没有这个文件，按照现有默认行为执行指令。
4. 注册一个新的专家mask_manager，它的职责：能够按照用户要求切换工作目录；通过skill_mask.exe读取、修改、删除当前文件夹内的cmd_masks文件；列出所有可用的指令列表和用户交互（这需要新的实现）。先把它设计为和当前其它专家一样单发启动。后续再考虑添加交互-执行混合循环。